# AgentOrchestra 成员气泡 UI 实施计划（Plan M1）

> **For agentic workers:** REQUIRED SUB-SKILL: Load the `sp-subagent-driven-development` skill (recommended) or `sp-executing-plans` skill via the `skill` tool to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在主对话流里把团队成员的交互渲染成独立成员气泡节点（名字/角色/头像 + 原文折叠展开），纯前端增量、后端零改动，并严格贯彻防崩溃（错误边界 + 数据兜底）。

**Architecture:** 复用现有 conversation-node 折叠机制。新增两个节点定义折叠 `orchestra_send_message` / `orchestra_update_task` 的 tool/call+result 事件 → 成员气泡 data；新增 AgentOrchestraBubble 渲染组件 + BubbleErrorBoundary 错误边界。核心「从 tool/call 参数解析气泡 data」做成纯函数（bubble-pure.ts，无 DSH/react 依赖，可 node 测）；渲染组件防御性写法。全部在浏览器侧，后端零改动。

**Tech Stack:** TypeScript + React（浏览器侧 client）。验证采用"宿主编译（host）+ client 语法检查 + 纯逻辑 node 单测"双轨。

**前置**：main 含 B1+B2。本计划在分支 `feat/bubble-ui` 进行。

---

## 范围（Scope）

5 个 Task：
1. 纯解析函数（bubble-pure.ts：parseSendMessageBubble / parseUpdateTaskBubble / collapseText）
2. 节点定义（member-message + task-done 的 conversation-node）+ 单测
3. AgentOrchestraBubble 渲染组件 + 折叠展开
4. BubbleErrorBoundary + index.tsx 注册
5. 全量验证收尾

## Non-Goals
- 实时推送 / 后端改动 / 改现有 AgentOrchestraCard + ActivityPanel。

---

## File Structure

| 文件 | 动作 |
|---|---|
| `src/client/bubble-pure.ts` | Create：纯解析/折叠函数（无 DSH/react 依赖，node 可测） |
| `src/client/agent-orchestra-bubble-definition.ts` | Create：conversation-node 定义 + ChatNodeDataMap 声明 + re-export bubble-pure |
| `src/client/AgentOrchestraBubble.tsx` | Create：气泡渲染组件 |
| `src/client/AgentOrchestraBubble.module.css` | Create：局部样式 |
| `src/client/bubble-error-boundary.tsx` | Create：错误边界 |
| `src/client/index.tsx` | Modify：注册两个节点 + 气泡渲染器 |
| `tests/bubble.test.mjs` | Create：纯逻辑单测 |

---

## Task 1: 纯解析函数（bubble-pure.ts）

**Files:**
- Create: `src/client/bubble-pure.ts`
- Test: `tests/bubble.test.mjs`（本 Task 先建纯函数，Task 2 写测试）

- [ ] **Step 1: 创建 `src/client/bubble-pure.ts`（纯 TS，无任何 DSH/react 依赖）**

```typescript
/** AgentOrchestra conversation bubble node data (pure, no DSH/react deps). */
export interface AgentOrchestraBubbleData {
  readonly kind: 'member-message' | 'task-done'
  readonly fromMember: string
  readonly fromRole: string
  readonly fromId: string
  readonly toMember?: string
  readonly taskId?: string
  readonly taskSubject?: string
  readonly text: string
  readonly ts: number
}

function str(v: unknown): string {
  return typeof v === 'string' ? v : ''
}

/** Parse orchestra_send_message tool/call args → bubble data (pure). */
export function parseSendMessageBubble(args: unknown, fromFallback = 'captain', ts = Date.now()): AgentOrchestraBubbleData | undefined {
  if (typeof args !== 'object' || args === null) return undefined
  const a = args as Record<string, unknown>
  const to = str(a.to)
  const content = str(a.content)
  if (to === '' && content === '') return undefined
  const from = str(a.from) || fromFallback
  return {
    kind: 'member-message',
    fromMember: from,
    fromRole: '',
    fromId: '',
    toMember: to === '' ? undefined : to,
    text: content,
    ts,
  }
}

/** Parse orchestra_update_task (status completed) args → bubble data (pure). */
export function parseUpdateTaskBubble(args: unknown, fromFallback = 'captain', ts = Date.now()): AgentOrchestraBubbleData | undefined {
  if (typeof args !== 'object' || args === null) return undefined
  const a = args as Record<string, unknown>
  if (str(a.status) !== 'completed') return undefined
  const taskId = str(a.task_id)
  const output = str(a.output)
  if (taskId === '' && output === '') return undefined
  return {
    kind: 'task-done',
    fromMember: fromFallback,
    fromRole: '',
    fromId: '',
    taskId: taskId === '' ? undefined : taskId,
    taskSubject: '',
    text: output,
    ts,
  }
}

/** Default excerpt length for collapsed bubble text. */
export const BUBBLE_EXCERPT_LEN = 200

/** Collapse long text to excerpt + whether truncated (pure). */
export function collapseText(text: string, max = BUBBLE_EXCERPT_LEN): { excerpt: string; truncated: boolean } {
  if (text.length <= max) return { excerpt: text, truncated: false }
  return { excerpt: text.slice(0, max) + '…', truncated: true }
}
```

- [ ] **Step 2: 用 Cursor TS 把纯函数转译成可被 node 测试的 .mjs（本环境关键步骤）**

因为 `src/client/` 被 host tsconfig 排除，且本机无 react 类型，用 `transpileModule` 把 `bubble-pure.ts` 转成临时 `tests/.bubble-pure.mjs`（仅该纯函数文件，无 react/dsh 依赖），供单测 import。

```bash
node -e "
const ts = require('C:/Users/Administrator/AppData/Local/Programs/cursor/resources/app/extensions/node_modules/typescript/lib/typescript.js');
const fs = require('fs');
const src = fs.readFileSync('src/client/bubble-pure.ts', 'utf8');
const out = ts.transpileModule(src, { compilerOptions: { module: ts.ModuleKind.ESNext, target: ts.ScriptTarget.ES2022, esModuleInterop: true }, fileName: 'bubble-pure.ts', reportDiagnostics: true });
if (out.diagnostics && out.diagnostics.length) { for (const d of out.diagnostics) console.log('DIAG', ts.flattenDiagnosticMessageText(d.messageText, ' ')); process.exit(1); }
fs.writeFileSync('tests/.bubble-pure.mjs', out.outputText);
console.log('transpiled to tests/.bubble-pure.mjs');
"
```

- [ ] **Step 3: 提交**

```bash
git add src/client/bubble-pure.ts
git commit -m "feat(bubble): add pure parse/collapse helpers (bubble-pure)"
```

---

## Task 2: 节点定义 + 单测

**Files:**
- Create: `src/client/agent-orchestra-bubble-definition.ts`
- Create: `tests/bubble.test.mjs`

- [ ] **Step 1: 创建 `src/client/agent-orchestra-bubble-definition.ts`**

先只放 ChatNodeDataMap 声明 + re-export（conversation-node 具体定义本环境无法完整编译 react 依赖，先声明 + 导出供 client bundle 用；实际 match/start/update/buildViewNode 按 `agent-orchestra-card-definition.ts` 的模式写，但 node 单测只测 pure 函数）。

```typescript
/** AgentOrchestra conversation bubble node data. */
import type { AgentOrchestraBubbleData, parseSendMessageBubble, parseUpdateTaskBubble, collapseText, BUBBLE_EXCERPT_LEN } from './bubble-pure.ts'
export type { AgentOrchestraBubbleData }
export { parseSendMessageBubble, parseUpdateTaskBubble, collapseText, BUBBLE_EXCERPT_LEN } from './bubble-pure.ts'

declare module '@deepseek-ai/dsh-client-ui-conversation/client' {
  interface ChatNodeDataMap {
    /** One member's interaction bubble in the conversation. */
    'agent-orchestra-bubble': AgentOrchestraBubbleData
  }
}
```

> 说明：本 Task 先建类型声明 + re-export（保证 client 类型可用）。完整的 conversation-node `match/start/update/buildViewNode` 定义是浏览器 bundle 运行时逻辑，本环境无法编译完整 react 依赖；在真实 build 环境会由 client tsconfig 编译。**如 implementer 判断可写出完整节点定义（不依赖 react 运行时、仅依赖 client-runtime 类型），可一并写出**；否则先保证纯函数 + 声明 + re-export 落地，节点定义 body 留给真实 client 构建（在计划里注明留待 e2e client 构建验证）。

- [ ] **Step 2: 创建 `tests/bubble.test.mjs`（6 条）**

```javascript
import assert from 'node:assert/strict'
// 从 transpile 出的纯函数模块导入
import { parseSendMessageBubble, parseUpdateTaskBubble, collapseText } from './.bubble-pure.mjs'

let passed = 0
function t(name, fn) { try { fn(); passed++; console.log('  PASS ' + name) } catch (e) { console.error('  FAIL ' + name + ': ' + e.message); process.exitCode = 1 } }

t('parse send_message bubble', () => {
  const b = parseSendMessageBubble({ to: 'alice', content: 'hello', from: 'bob' })
  assert.equal(b?.kind, 'member-message')
  assert.equal(b?.fromMember, 'bob')
  assert.equal(b?.toMember, 'alice')
  assert.equal(b?.text, 'hello')
})
t('send_message missing from falls back to captain', () => {
  const b = parseSendMessageBubble({ to: 'alice', content: 'hi' })
  assert.equal(b?.fromMember, 'captain')
})
t('update_task non-completed yields undefined', () => {
  assert.equal(parseUpdateTaskBubble({ task_id: 't1', status: 'in_progress' }), undefined)
})
t('update_task completed bubble', () => {
  const b = parseUpdateTaskBubble({ task_id: 't1', status: 'completed', output: 'done the thing' })
  assert.equal(b?.kind, 'task-done')
  assert.equal(b?.taskId, 't1')
  assert.equal(b?.text, 'done the thing')
})
t('collapseText short stays', () => {
  assert.deepStrictEqual(collapseText('short'), { excerpt: 'short', truncated: false })
})
t('collapseText long truncates with ellipsis', () => {
  const long = 'x'.repeat(300)
  const r = 

- [ ] **Step 3: 跑单测**

```bash
# 先确保 tests/.bubble-pure.mjs 存在（Task1 Step2 生成过）
node tests/bubble.test.mjs
```
Expected: 6 条 PASS。

- [ ] **Step 4: 提交**

```bash
git add src/client/agent-orchestra-bubble-definition.ts tests/bubble.test.mjs
git commit -m "feat(bubble): node definition type + pure unit tests"
```

> 注意：`tests/.bubble-pure.mjs` 是转译的临时产物，由 implementer 决定是否提交（建议保留在 tests/ 以便测试可跑）。

---

## Task 3: AgentOrchestraBubble 渲染组件

**Files:**
- Create: `src/client/AgentOrchestraBubble.tsx`
- Create: `src/client/AgentOrchestraBubble.module.css`

- [ ] **Step 1: 创建 `src/client/AgentOrchestraBubble.tsx`（防御性）**

```tsx
import { useState } from 'react'
import type { AgentOrchestraBubbleData } from './agent-orchestra-bubble-definition.ts'
import { collapseText } from './bubble-pure.ts'
import css from './AgentOrchestraBubble.module.css'

export interface BubbleProps { data: AgentOrchestraBubbleData }

/** One member's interaction bubble. Defensive: never throws on bad data. */
export function AgentOrchestraBubble({ data }: BubbleProps) {
  const [expanded, setExpanded] = useState(false)
  const member = data?.fromMember ? data.fromMember : '未知成员'
  const role = data?.fromRole ? (' · ' + data.fromRole) : ''
  const text = (data?.text ?? '').trim()
  const { excerpt, truncated } = collapseText(text)
  const body = (truncated && !expanded) ? excerpt : (text || '（无正文）')
  const time = data?.ts ? new Date(data.ts).toLocaleTimeString() : ''
  const meta = data?.kind === 'member-message'
    ? ('→ ' + (data.toMember || 'captain'))
    : (data?.kind === 'task-done' ? ('完成任务 ' + (data.taskId || '') + (data.taskSubject ? ': ' + data.taskSubject : '')) : '')
  return (
    <div className={css.root} data-bubble-kind={data?.kind ?? ''} data-bubble-from={data?.fromMember ?? ''}>
      <div className={css.head}>
        <span className={css.avatar}>{member.trim().slice(0, 1).toUpperCase() || '?'}</span>
        <span className={css.name}>{member}</span>
        <span className={css.role}>{role}</span>
        <span className={css.time}>{time}</span>
      </div>
      <div className={css.meta}>{meta}</div>
      <div className={css.body} data-bubble-text>{body}</div>
      {truncated && (
        <button type="button" className={css.toggle} onClick={() => setExpanded((v) => !v)}>
          {expanded ? '收起' : '展开全文'}
        </button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 创建 `src/client/AgentOrchestraBubble.module.css`**

```css
.root { display: flex; flex-direction: column; gap: 4px; padding: 8px 12px; margin: 4px 0; border-radius: 8px; border-left: 3px solid #3b82f6; background: #f8fafc; font-family: inherit; }
.root[data-bubble-kind='task-done'] { border-left-color: #22c55e; }
.head { display: flex; align-items: center; gap: 6px; }
.avatar { width: 22px; height: 22px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; background: #e2e8f0; font-size: 12px; font-weight: 600; color: #334155; }
.name { font-weight: 600; font-size: 13px; color: #1e293b; }
.role { font-size: 11px; color: #64748b; }
.time { margin-left: auto; font-size: 10px; color: #94a3b8; }
.meta { font-size: 11px; color: #64748b; }
.body { font-size: 13px; color: #334155; white-space: pre-wrap; word-break: break-word; }
.toggle { align-self: flex-start; background: none; border: none; color: #2563eb; cursor: pointer; font-size: 12px; padding: 0; }
```

- [ ] **Step 3: client 语法检查（含新组件）**

```bash
node -e "
const ts = require('C:/Users/Administrator/AppData/Local/Programs/cursor/resources/app/extensions/node_modules/typescript/lib/typescript.js');
const fs = require('fs');
const files = ['src/client/AgentOrchestraBubble.tsx','src/client/AgentOrchestraBubble.module.css','src/client/agent-orchestra-bubble-definition.ts','src/client/bubble-pure.ts'];
let err = 0;
for (const f of files) {
  const s = fs.readFileSync(f, 'utf8');
  const o = ts.transpileModule(s, { compilerOptions: { module: ts.ModuleKind.ESNext, target: ts.ScriptTarget.ES2022, jsx: ts.JsxEmit.ReactJSX }, fileName: f, reportDiagnostics: true });
  for (const d of (o.diagnostics || [])) { err++; console.log('SYNTAX', f, ts.flattenDiagnosticMessageText(d.messageText, ' ')); }
}
console.log('client syntax', err === 0 ? 'OK' : (err + ' err'));
process.exit(err ? 1 : 0);
"
```
Expected: OK。

- [ ] **Step 4: 提交**

```bash
git add src/client/AgentOrchestraBubble.tsx src/client/AgentOrchestraBubble.module.css
git commit -m "feat(bubble): add AgentOrchestraBubble defensive render component"
```

---

## Task 4: BubbleErrorBoundary + index.tsx 注册

**Files:**
- Create: `src/client/bubble-error-boundary.tsx`
- Modify: `src/client/index.tsx`

- [ ] **Step 1: 创建 `src/client/bubble-error-boundary.tsx`**

```tsx
import { Component, type ReactNode } from 'react'

export interface BubbleErrorBoundaryProps { children: ReactNode }
export interface BubbleErrorBoundaryState { hasError: boolean }

/** Isolate one bubble's render failure to a placeholder, never crashing the app. */
export class BubbleErrorBoundary extends Component<BubbleErrorBoundaryProps, BubbleErrorBoundaryState> {
  state: BubbleErrorBoundaryState = { hasError: false }
  static getDerivedStateFromError(): BubbleErrorBoundaryState { return { hasError: true } }
  render(): ReactNode {
    if (this.state.hasError) {
      return <div data-bubble-error>（该成员气泡渲染失败）</div>
    }
    return this.props.children
  }
}
```

- [ ] **Step 2: index.tsx 注册气泡渲染器**

修改 `src/client/index.tsx`：
1. import `AgentOrchestraBubble`、`BubbleErrorBoundary`、`AgentOrchestraBubbleData`。
2. 在 `apply` 里、现有卡片注册之后追加：

```tsx
  ctx.slots.inject('conversation.chat.node', () => ctx.slots.register({
    name: 'conversation.chat.node',
    key: 'agent-orchestra-bubble',
  }, (props: { data: AgentOrchestraBubbleData }) => (
    <BubbleErrorBoundary><AgentOrchestraBubble data={props.data} /></BubbleErrorBoundary>
  )))
```

> 对齐同文件现有 `key: 'agent-orchestra'` 的卡片注册方式。

- [ ] **Step 3: 宿主编译 + client 语法检查**

Run 宿主编译命令，再跑 Task 3 Step 3 语法命令（含 index.tsx）。Expected: 都通过。

- [ ] **Step 4: 提交**

```bash
git add src/client/bubble-error-boundary.tsx src/client/index.tsx
git commit -m "feat(bubble): add error boundary and register bubble node"
```

---

## Task 5: 全量验证收尾

**Files:**
- 验证 + 可能微调

- [ ] **Step 1: 宿主编译（host）**

Run 宿主编译命令。Expected: pre 0。

- [ ] **Step 2: client 全量语法检查**

对 `src/client/*` 全部 .ts/.tsx/.css 跑 transpileModule。Expected: 0 错误。

- [ ] **Step 3: 气泡纯逻辑单测**

Run `node tests/bubble.test.mjs`。Expected: 6 条 PASS。

- [ ] **Step 4: 既有测试回归**

Run: workflow / persona / orchestrator / b2-complete。Expected: 全过（M1 不改后端）。

- [ ] **Step 5: 提交（如有微调）**

```bash
git add -A
git commit -m "test(bubble): final verification"
```

---

## 验证与验收

1. 宿主编译 0 诊断。
2. client 全部文件语法检查通过。
3. bubble 纯逻辑 6 条 PASS。
4. 既有 34 项测试回归全过。
5. （可选真实环境）web 稳定性：气泡与活动面板/团队卡并存不崩；单泡数据缺失降级占位。

## Non-Goals
- 实时推送 / 后端改动 / 改现有 AgentOrchestraCard + ActivityPanel。

## Self-Review

**1. Spec 覆盖**：纯前端 + 错误边界 + 数据兜底 + 只增不改 + 纯函数 + CSS 隔离 → Task1-4；数据模型/节点定义 → Task1-2；渲染折叠展开 → Task3；错误边界 + 注册 → Task4；验证 → Task5。✅
**2. Placeholder scan**：无 TBD（Task2 注明的"节点定义完整 body 留待真实 client 构建"属环境限制，非占位）。✅
**3. Type consistency**：AgentOrchestraBubbleData 在 bubble-pure 定义、definition re-export、组件与注册引用一致；parse/collapse 签名一致；BUBBLE_EXCERPT_LEN=200 与 collapseText 默认一致；单测断言与实现一致。✅
